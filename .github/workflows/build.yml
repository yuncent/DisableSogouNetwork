name: Build and Release DisableSogouNetwork

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write   

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set version
      shell: bash
      run: |
        if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
          echo "VERSION=${GITHUB_REF_NAME}" >> $GITHUB_ENV
        else
          echo "VERSION=1.0.0-$(git rev-parse --short HEAD)" >> $GITHUB_ENV
        fi

    - name: Build
      shell: cmd
      run: |
        for /f "usebackq delims=" %%i in (`"C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath`) do set VS_PATH=%%i
        call "%VS_PATH%\Common7\Tools\VsDevCmd.bat" -arch=amd64

        cl.exe /EHsc /std:c++17 /O2 /MT ^
          /DVERSION="%VERSION%" ^
          /Fe:"DisableSogouNetwork_%VERSION%.exe" ^
          DisableSogouNetwork.cpp ^
          /link ole32.lib oleaut32.lib uuid.lib advapi32.lib shell32.lib user32.lib comctl32.lib gdi32.lib ^
          /SUBSYSTEM:WINDOWS

    - name: Zip
      shell: powershell
      run: |
        Compress-Archive -Path "DisableSogouNetwork_${env:VERSION}.exe" `
        -DestinationPath "DisableSogouNetwork_${env:VERSION}.zip"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: build
        path: "*.zip"

    - name: Release (ONLY TAG)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: "*.zip"
        generate_release_notes: true
